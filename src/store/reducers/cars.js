const initialState = {
  cars : []
};

const reducer = (state = initialState , action) => {
  switch (action.type) {
    case :

      
    default:
      return state;
  }
};

export default reducer;
